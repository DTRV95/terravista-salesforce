<?xml version="1.0" encoding="UTF-8"?>
<Flow xmlns="http://soap.sforce.com/2006/04/metadata">
    <apiVersion>62.0</apiVersion>
    <description>Quando uma chamada e dada por concluida num lead, carimba a Data do 1o Contacto. E este Flow que alimenta a metrica de latencia: sem ele o campo so existia porque o script de dados o escrevia, e numa org real ficaria sempre vazio.</description>
    <environments>Default</environments>
    <interviewLabel>Carimbar Primeiro Contacto {!$Flow.CurrentDateTime}</interviewLabel>
    <label>Carimbar Primeiro Contacto</label>
    <processType>AutoLaunchedFlow</processType>
    <recordUpdates>
        <name>Carimbar_o_lead</name>
        <label>Carimbar o lead</label>
        <locationX>176</locationX>
        <locationY>158</locationY>
        <filterLogic>and</filterLogic>
        <filters>
            <field>Id</field>
            <operator>EqualTo</operator>
            <value>
                <elementReference>$Record.WhoId</elementReference>
            </value>
        </filters>
        <filters>
            <field>Data_Primeiro_Contacto__c</field>
            <operator>IsNull</operator>
            <value>
                <booleanValue>true</booleanValue>
            </value>
        </filters>
        <inputAssignments>
            <field>Data_Primeiro_Contacto__c</field>
            <value>
                <elementReference>$Flow.CurrentDateTime</elementReference>
            </value>
        </inputAssignments>
        <object>Lead</object>
    </recordUpdates>
    <start>
        <locationX>50</locationX>
        <locationY>0</locationY>
        <connector>
            <targetReference>Carimbar_o_lead</targetReference>
        </connector>
        <!--
          So conta uma chamada CONCLUIDA. Uma tarefa aberta nao prova contacto
          nenhum, e era isso que o LastActivityDate deixava passar.

          O filtro do Id na atualizacao resolve sozinho o caso de a tarefa estar
          num Contacto e nao num Lead: nao encontra nenhum Lead e nao faz nada.
          Evita ter de comparar prefixos de Id em formula.
        -->
        <filterFormula>AND(
  ISPICKVAL({!$Record.Status}, "Completed"),
  ISPICKVAL({!$Record.TaskSubtype}, "Call"),
  NOT(ISBLANK({!$Record.WhoId}))
)</filterFormula>
        <object>Task</object>
        <recordTriggerType>CreateAndUpdate</recordTriggerType>
        <triggerType>RecordAfterSave</triggerType>
    </start>
    <status>Active</status>
</Flow>
